export default function taskBlock(trueOrFalse) {
    const task = false;
    const task2 = true;
  
    if (trueOrFalse) {
      // Perform operations or return task/task2 if needed
    }
  
    return [task, task2];
  }
  